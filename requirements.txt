# Core pipeline
edgartools>=5.30.0          # SEC EDGAR filing access + XBRL financials
openai>=1.69.0              # GPT-5.4 Responses API
tiktoken>=0.9.0             # Token counting for cost estimation
python-dotenv>=1.0.0        # .env file loading

# Transcript scraping
selenium>=4.20.0            # Headless Chrome for roic.ai + PDF generation
selenium-stealth>=1.0.6     # Anti-detection for scraping
html2text>=2024.2.26        # HTML to markdown conversion

# Report generation
markdown>=3.6               # Markdown to HTML for final report
